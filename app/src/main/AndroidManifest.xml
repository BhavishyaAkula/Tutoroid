<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akulabhavishya.tutoroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/splashimg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splashimg"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Belogin"
            android:label="Tutor Login" />
        <activity
            android:name=".Findlog"
            android:label="Student Login" />
        <activity
            android:name=".Findreg"
            android:label="Student Register" />
        <activity android:name=".Behome" />
        <activity
            android:name=".Bereg"
            android:label="Tutor Register" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Viewrequest"
            android:label="Tutor View Request" />
        <activity
            android:name=".Update"
            android:label="Tutor Profile Update" />
        <activity
            android:name=".Tutor_RetreiveActivity"
            android:label="Tutors List" />
        <activity
            android:name=".TutorDetailsActivity"
            android:label="Tutor Details" />
        <activity
            android:name=".SendingSmsActivity"
            android:label="Sendind SMS" />
        <activity
            android:name=".SendingMailActivity"
            android:label="Sending Mail" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".AddRequestActivity"
            android:label="Add Request" />
        <activity
            android:name=".TutorResponseActivity"
            android:label="Tutor Response" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ContactActivity"></activity>
    </application>

</manifest>